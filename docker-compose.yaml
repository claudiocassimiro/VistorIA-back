services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - POETRY_VIRTUALENVS_CREATE=false
    volumes:
      - ./vistoria_back:/app/vistoria_back 
    networks:
      - app-network

  nginx:
    image: nginx:latest
    ports:
      - "80:80"  # Mapeia a porta 8080 do host para a porta 80 do container
      - "443:443"  # Mapeia a porta 8443 do host para a porta 443 do container
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
      - /home/tensordock/projects/VistorIA-back/nginx/webroot:/home/tensordock/projects/VistorIA-back/nginx/webroot
    networks:
      - app-network
    depends_on:
      - app

networks:
  app-network:
    driver: bridge
